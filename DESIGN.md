# DESIGN.md — Crewmon 디자인 시스템 (Premium Monotone)

> 이 문서는 Crewmon UI의 **디자인 단일 출처**다. 컬러·타이포·간격·모서리·모션의 기준값을 정의하고,
> 코드(`globals.css` 토큰)와 1:1로 매칭한다. 새 화면/컴포넌트는 이 토큰만 쓰고 하드코딩 색을 금한다.

## 1. 컨셉 — Premium Monotone (의도적 흑백감)

소상공인 매장(카페·리테일)의 점주·알바 크루가 매일 여는 앱. **정제되고 또렷하며 신뢰감 있는** 톤.
거의 흰색에 가까운 아이보리 종이 위에 잉크로 쓴 듯한 모노톤. 색은 절제하고 위계는 타이포·명도·여백으로.

- **키워드:** 거의 흰 아이보리 · 잉크 블랙 · 보이는 헤어라인 경계 · 대담한 타이포 · 무채도 절제
- **회피:** 형광·고채도 색 · 의미 없는 컬러 · 모호하게 떠 있는 카드(약한 그림자만) · 빽빽한 밀도
- **색 정책:** 강조·CTA·중요도는 잉크(near-black)로. 상태색(그린/블루)은 저채도로만, 의미 구분이 꼭 필요할 때만(캘린더 연장 +그린 / 부족 -그레이). 차트도 잉크+저채도 세이지.

## 2. 컬러 토큰

> 구현 메모: 토큰 **이름은 기존 유지**(`--coral` 등)하되 **값만 교체**한다.
> 컴포넌트가 의미 클래스(`bg-coral`/`text-muted`/`bg-surface`/`border-border`)를 쓰므로 값 교체만으로 전체 전파(리팩터 0).
> 이름상 `coral`은 이제 **잉크 near-black(#15110D)**을 가리킨다(시맨틱 라벨일 뿐, 기능 동일).

| 토큰 | 값 | 역할 |
|---|---|---|
| `--background` | `#FCFBF7` | 앱 전체 배경 — 거의 흰색에 가까운 아이보리(미세 웜) |
| `--surface` | `#FFFFFF` | 카드/시트 표면 — 순백. 배경과 거의 같으므로 **경계는 `--border`로** 분리 |
| `--foreground` | `#1C1814` | 본문 텍스트 — 잉크에 가깝게(대비 강화, 순흑은 회피) |
| `--muted` | `#8B8178` | 보조 텍스트 — 뉴트럴 토프 |
| `--border` | `#E8E1D4` | **헤어라인 경계** — 카드 윤곽·구획선·divider. Premium Monotone의 핵심(카드가 떠 있지 않게) |
| `--coral` | `#15110D` | **메인 강조(잉크)** — CTA·선택·브랜드·중요도. near-black |
| `--coral-soft` | `#F1ECE2` | 미세 채움 — 칩·비활성·소프트 강조 배경 |
| `--green` | `#5B7A63` | 상태: 정상/연장 — **저채도 세이지**(형광 회피) |
| `--green-soft` | `#EAEEE8` | 그린 소프트 배경 |
| `--blue` | `#4A5A6E` | 상태: 연장/주휴 — **저채도 슬레이트** |
| `--gray-badge` | `#6F6760` | 중립 배지 — 무채 다크그레이 |

**캘린더 배지 규칙(불변):** 그린 `+N분`=연장, 회색 `-N분`=부족, `+0분` 숨김 — 색 의미는 유지하되 위 **저채도** 톤으로.
**상태색 사용 원칙:** 기본은 잉크/그레이 명도로 구분. 그린/블루는 의미 구분이 꼭 필요한 곳(캘린더 연장, 대타, 근무중 점)에서만 절제해서. 매니저·중요 강조는 잉크(`--coral`).

## 3. 타이포그래피

- **Pretendard Variable 적용됨** — 자체 호스팅(`src/app/fonts/PretendardVariable.woff2`, `next/font/local`, weight 45~920). `--font-sans`가 가리킴.
- 위계: 화면 타이틀 `text-2xl/font-bold` · 큰 숫자(급여/시간) `text-3xl~4xl/font-bold/tabular-nums` · 본문 `text-sm~base` · 보조 `text-xs text-muted`.
- 숫자는 항상 `tabular-nums`로 정렬(금액·시간 표).

## 4. 모서리·그림자·경계·간격

- **모서리:** 에디토리얼 느낌으로 한 단계 조임 — 카드/시트/패널 `rounded-2xl`, 버튼 `rounded-2xl`, 칩/배지 `rounded-full`.
- **경계(핵심):** 모든 카드·패널·divider는 **보이는 헤어라인** `border border-border`. 배경(거의 흰색)과 표면(순백)이 가까우므로 경계가 카드를 정의한다. 흐린 `border-foreground/5`는 금지(→ `border-border`).
- **그림자:** 절제. 경계로 분리하고 그림자는 거의 안 보이게 — `shadow-[0_1px_2px_rgba(28,24,20,0.04)]`. 떠 있는 느낌의 큰 그림자 금지.
- **타이포 위계:** 화면/섹션 헤더 `text-lg~2xl font-bold tracking-tight`, 큰 숫자 `tabular-nums`. 모노톤이라 위계는 크기·굵기로.
- **간격:** 여백 넉넉히. 카드 패딩 `p-5`, 섹션 간 `gap-4~7`, 화면 좌우 `px-5`.

## 5. 모션

- 부드럽고 느긋하게: 전환 `transition` `duration-200~300` `ease-out`. 바텀시트는 spring 느낌의 ease.
- 과한 바운스·플래시 금지(편안함 우선).

## 6. 컴포넌트 가이드(요지)

- **Card**: 웜 화이트 표면 + 큰 모서리 + 웜 소프트 섀도. 요약 카드는 `coral-soft` 배경 강조.
- **BottomNav**: 활성 탭 블랙, 비활성 muted. 배경 surface + 상단 헤어라인.
- **Button(주요)**: 블랙 배경/화이트 텍스트, `rounded-2xl`, 큰 탭 타깃.
- **StatusBadge**: 정상=그린소프트, 연장/주휴=블루, 결근/부족=그레이배지, 휴가=뉴트럴. 모두 `rounded-full` pill.

## 7. 작업 순서(Premium Monotone 리디자인)

1. ✅ 방향 확정(Premium Monotone) — 의도적 흑백감, 잉크 강조
2. ✅ 토큰 재정의(`globals.css`) — `--border` 신설, 배경 거의 흰색, foreground 잉크화, green/blue 저채도화
3. ✅ 경계·모서리 패스 — 전역 `border-border`(보이는 헤어라인), `rounded-2xl`, 그림자 절제
4. ✅ 하드코딩 색 → 토큰 sweep — amber/emerald/neutral/blue 전부 제거, 차트(잉크+세이지)·레이더(잉크) 포함
5. ✅ 화면별 마감(홈 히어로 재구성, 스케쥴 아바타 무채도 명도 사다리, 마이페이지 문서함 통일, 집계 차트)

> 참조 샘플(`public/sample/IMG_3606~3619.png`)은 **기존(=바꾸기 전) 디자인**이다. 이 문서가 새 기준이다.
