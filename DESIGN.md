# DESIGN.md — Crewmon 디자인 시스템 (Warm Soft)

> 이 문서는 Crewmon UI의 **디자인 단일 출처**다. 컬러·타이포·간격·모서리·모션의 기준값을 정의하고,
> 코드(`globals.css` 토큰)와 1:1로 매칭한다. 새 화면/컴포넌트는 이 토큰만 쓰고 하드코딩 색을 금한다.

## 1. 컨셉 — Warm Soft (친근·편안)

소상공인 매장(카페·리테일)의 점주·알바 크루가 매일 여는 앱. **따뜻하고 편안하며 신뢰감 있는** 톤.
차갑고 기계적인 핀테크가 아니라, "내 일터의 다정한 도구" 느낌.

- **키워드:** 크림빛 · 블랙 포인트 · 둥글고 부드러움 · 여백 · 손에 잡히는 따뜻함
- **회피:** 형광·고채도 · 날카로운 직각 · 차가운 순수 그레이/블루 · 빽빽한 정보 밀도

## 2. 컬러 토큰

> 구현 메모: 토큰 **이름은 기존 유지**(`--coral` 등)하되 **값만 Warm Soft로 교체**한다.
> 컴포넌트가 의미 클래스(`bg-coral`/`text-muted`/`bg-surface`)를 쓰므로 값 교체만으로 전체 전파(리팩터 0).
> 이름상 `coral`은 이제 **블랙(#1A1A1A)**을 가리킨다(시맨틱 라벨일 뿐, 기능 동일).

| 토큰 | 값 | 역할 |
|---|---|---|
| `--background` | `#FAF4EC` | 앱 전체 배경 — 웜 크림 |
| `--surface` | `#FFFCF7` | 카드/시트 표면 — 웜 화이트 |
| `--foreground` | `#2A2320` | 본문 텍스트 — 웜 다크브라운(순흑 회피) |
| `--muted` | `#9A8C82` | 보조 텍스트 — 웜 토프 |
| `--coral` | `#1A1A1A` | **메인 강조(블랙)** — CTA·선택·브랜드. near-black(순흑 부담 완화) |
| `--coral-soft` | `#ECE7E0` | 강조 배경 — 웜 그레이(요약카드·선택칩). 크림+블랙 모노톤 |
| `--green` | `#5E9A6B` | 상태: 정상/정규 — 세이지 그린 |
| `--green-soft` | `#E7EFE2` | 그린 배경 |
| `--blue` | `#4F7CA8` | 상태: 연장/주휴 — 더스티 블루(웜) |
| `--gray-badge` | `#8A7C72` | 중립 배지 — 웜 그레이 |

**캘린더 배지 규칙(불변):** 그린 `+N분`=연장, 회색 `-N분`=부족, `+0분` 숨김 — 색 의미는 유지하되 위 웜 톤으로.

## 3. 타이포그래피

- **Pretendard Variable 적용됨** — 자체 호스팅(`src/app/fonts/PretendardVariable.woff2`, `next/font/local`, weight 45~920). `--font-sans`가 가리킴.
- 위계: 화면 타이틀 `text-2xl/font-bold` · 큰 숫자(급여/시간) `text-3xl~4xl/font-bold/tabular-nums` · 본문 `text-sm~base` · 보조 `text-xs text-muted`.
- 숫자는 항상 `tabular-nums`로 정렬(금액·시간 표).

## 4. 모서리·그림자·간격

- **모서리:** Warm Soft의 핵심. 카드/시트/버튼을 한 단계 더 둥글게 — 카드 `rounded-3xl`(현 `rounded-2xl`에서↑), 버튼 `rounded-2xl`, 칩/배지 `rounded-full`.
- **그림자:** 차가운 검정 그림자 대신 **웜 소프트 섀도** — `shadow-[0_2px_16px_rgba(82,60,40,0.06)]`. 테두리는 `border-black/5` 대신 `border-[#2A2320]/5`.
- **간격:** 여백 넉넉히. 카드 패딩 `p-5`, 섹션 간 `gap-4~6`, 화면 좌우 `px-5~6`.

## 5. 모션

- 부드럽고 느긋하게: 전환 `transition` `duration-200~300` `ease-out`. 바텀시트는 spring 느낌의 ease.
- 과한 바운스·플래시 금지(편안함 우선).

## 6. 컴포넌트 가이드(요지)

- **Card**: 웜 화이트 표면 + 큰 모서리 + 웜 소프트 섀도. 요약 카드는 `coral-soft` 배경 강조.
- **BottomNav**: 활성 탭 블랙, 비활성 muted. 배경 surface + 상단 헤어라인.
- **Button(주요)**: 블랙 배경/화이트 텍스트, `rounded-2xl`, 큰 탭 타깃.
- **StatusBadge**: 정상=그린소프트, 연장/주휴=블루, 결근/부족=그레이배지, 휴가=뉴트럴. 모두 `rounded-full` pill.

## 7. 작업 순서(이 리디자인)

1. ✅ 방향 확정(Warm Soft) → 강조색은 블랙으로 조정
2. ✅ 컬러 토큰 교체(`globals.css`)
3. ✅ 모서리·그림자·간격 패스(전역 rounded-3xl, Card p-5/웜섀도, 웜 보더, 쿨틴트 웜화)
4. ✅ 화면별 마감(홈/출퇴근/급여/마이페이지/스케쥴)
5. ✅ Pretendard 폰트 도입

> 참조 샘플(`public/sample/IMG_3606~3619.png`)은 **기존(=바꾸기 전) 디자인**이다. 이 문서가 새 기준이다.
