# 02-approval — 승인 결정 (T8)

- **작성**: orchestrator (사용자 게이트)
- **대상**: `01-prd.md`
- **상태**: ✅ APPROVED (2026-06-01)

## 최종 사용자 결정: APPROVE

| Q | 결정 | architect/developer 지침 |
|---|---|---|
| Q-B UI 배치 | **마이페이지 전환 + `/master` 라우트** | 마이페이지에 계정/역할 전환 UI. 마스터 전용 `/master` 집계 라우트(크루 목록 + 각자 근무/휴일). 크루는 기존 4탭 그대로 본인 스코프. |
| Q-C 권한 경계 | **본인 데이터 강제 스코프** | 크루가 타 crewId/전체 요청 시 → 본인 crewId로 강제(타인 노출 0). 별도 403 대신 강제 스코프. 단 마스터 전용 액션(수락/집계/초대생성)은 크루 차단(아래). |
| Q-E 크루 수 | **크루 3명(김민정+2) + 마스터 1** | mock 시드: 마스터(점주) 1 + 크루 3(김민정 포함). 각 크루 근무기록/스케줄. |
| Q-A store 전략 | **전략 A (append+fallback)** — architect 확정 | crewId 인자 append + 기본값(김민정) fallback. 기존 호출 호환(회귀 0). ADR 0004 선례 계승. |
| Q-D 전달 방식 | architect 결정 (권장 헤더 `x-crew-id`/`x-role` 또는 쿼리) | mock이므로 클라가 현재 사용자 컨텍스트로 스코프 전달. 일관된 1가지 방식. |

## 마스터 전용 권한 게이트 (요구 4 핵심)
- **수락(approveRequest)**: role==='master'만. 크루는 UI 수락 버튼 숨김 + API 수락 거부(크루 호출 시 403 또는 무시). 크루는 수정요청 생성만.
- **마스터 집계뷰(/master)**: 마스터만 접근(크루 접근 시 차단/리다이렉트).
- **초대 생성**: 마스터만.

## 제약
- 기존 138 테스트 회귀 0(단일사용자 흐름 = '김민정 크루' 기본 보존). append-only 우선. RSC/client 경계. seed 불변식 보존(김민정 May 데이터 = 기존값).
- mock 역할 전환(실제 인증/DB 없음). 세션 = 클라 상태/localStorage.
